const gameReducer = (state, action) => {
  switch (action.type) {
    case 'ADD_GAME': {
      break;
    }
    case 'SET_STATUS_GAME': {
      break;
    }
    default: {
      return state;
    }
  }
}

export default gameReducer;